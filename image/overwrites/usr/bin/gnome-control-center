#!/bin/bash

USER=`id -un`

if [ $USER == "user" ]; then
  zenity --info --text="Systeminstillingerne er ikke tilgængelige for brugere\n Kontakt personalet hvis der er problemer"
else
  /usr/bin/gnome-control-center.real
fi
